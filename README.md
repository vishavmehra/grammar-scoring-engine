# grammar scoring engine
A hybrid speech assessment pipeline combining Wav2Vec 2.0 Transformer embeddings with handcrafted acoustic features (Librosa) to predict grammar proficiency scores using XGBoost and PCA.
